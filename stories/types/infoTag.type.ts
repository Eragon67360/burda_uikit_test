/**
 * @interface InfoTagArgs
 * @description Configuration options for creating an information tag component
 */
export type InfoTagArgs = {
  /**
   * The text content to be displayed within the tag
   * @required
   * @example
   * label: 'ab 3,40 € pro Ausgabe'
   */
  label: string;
};
